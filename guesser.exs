# 1. Принимаем целое число от пользователя и выполняем его парсинг
IO.gets("Enter number: ")
Integer.parse(some_string)

# 2. Генерируем случайное число в диапазоне, используя число из шага 1 как максимум
:rand.uniform(maximum)

# 3. Предлагаем пользователю угадать загаданное число путём ввода с клавиатуры

# 4. Если пользователь угадал, возвращаем загаданное число. Иначе просто возвращаем ошибку.
